import React from 'react';
import { render } from '@testing-library/react';
import FiltePricerDropdown from 'components/FiltePricerDropdown';

it('check Price DropDown Render', () => {
  render(<FiltePricerDropdown />);
});
